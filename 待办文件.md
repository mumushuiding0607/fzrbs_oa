# Finance 模块功能优化待办

> 计划陆续实现的功能优化清单

---

## 一、预算模块（budget）

### 高优先级
- [ ] **全局搜索** — 跨模块搜索：项目/合同/发票/订单编号
- [ ] **操作日志审计** — 关键数据修改留痕，支持按人员/时间查询
- [ ] **批量决算** — 支持批量选择多个项目一次性提交决算审批

### 中优先级
- [ ] **预算执行预警** — 当预算支出超过80%/90%/100%时自动提醒负责人
- [ ] **跨项目资金调拨** — 支持将一个项目的预算余额调拨至另一个项目
- [ ] **项目延期预警** — 根据合同期限自动提醒即将到期或已超期的项目
- [ ] **预算版本管理** — 支持预算变更历史记录，类似于合同变更



---

## 二、合同模块（contract）

### 高优先级

- [ ] **付款条件自动提醒** — 到期前N天自动提醒相关人员跟进回款
- [ ] **合同变更记录** — 记录合同金额、期限、付款条件的每一次变更

### 中优先级
- [ ] **合同到期预警** — 按月/季/年统计即将到期的合同数量
- [ ] **合同履行监控** — 设置里程碑节点，自动追踪项目进度与合同履约

### 低优先级
- [ ] **电子合同集成** — 对接电子签章系统实现在线签约
- [ ] **供应商合同管理** — 除客户合同外，增加采购/供应商合同管理

---

## 三、发票模块（invoice）



### 中优先级
- [ ] **进项销项抵扣分析** — 增值税抵扣分析，税负计算
- [ ] **红字发票申请流程** — 线上申请、审批、作废冲红
- [ ] **批量操作** — 批量审批、批量导出、批量打印

### 低优先级
- [ ] **发票真伪查验** — 集成税务局发票查验API
- [ ] **电子发票PDF自动归集** — 开票后自动生成并发送给客户邮箱

---

## 四、广告订单模块（order）

### 高优先级

### 中优先级
- [ ] **组合套餐销售** — 支持将多个版面/时段打包成套餐销售
- [ ] **优惠折扣规则引擎** — 根据客户等级/订单金额自动应用折扣
- [ ] **广告撤刊/改刊流程** — 标准化撤刊申请和费用计算

### 低优先级
- [ ] **广告效果追踪** — 上刊后关联广告监播/回款数据
- [ ] **订单自动续约提醒** — 根据合同期限自动提醒客户续约

---

## 五、用印模块（useseal）

### 高优先级
- [ ] **用印与合同绑定** — 必须关联合同才能申请用印（部分场景）
- [ ] **用印文件自动归档** — 用印审批通过后自动上传至对应项目/合同

### 中优先级
- [ ] **用印次数统计** — 按部门/人员统计用印频次
- [ ] **用印类型分级** — 区分公章/法人章/合同章等，不同类型不同审批流

### 低优先级
- [ ] **电子用印集成** — 对接电子签章系统实现在线盖章

---

## 六、考勤模块（attandance）

### 高优先级
- [ ] **完整考勤报表** — 出勤天数/迟到/早退/请假/加班统计
- [ ] **请假销假闭环** — 请假申请→审批→销假确认

### 中优先级
- [ ] **加班调休管理** — 加班时长累计、转调休、过期提醒
- [ ] **排班管理** — 支持轮班/倒班等特殊排班

### 低优先级
- [ ] **考勤数据推送** — 推送至工资系统参与薪资计算
- [ ] **异常打卡AI审核** — 疑似代打卡等异常行为标记提示

---

## 七、流程模板模块（Flowtemplate）

### 中优先级
- [ ] **加签/转交** — 审批人可临时加签或转交他人
- [ ] **委托代理** — 长期出差等场景可设置流程代理

### 低优先级
- [ ] **可视化流程设计器** — 拖拽式设计审批流程，节点可配置
- [ ] **流程版本管理** — 流程变更有版本记录，可回滚

---

## 八、跨模块通用功能

### 高优先级
- [ ] **全局搜索** — 跨模块搜索：项目/合同/发票/订单编号
- [ ] **消息中心** — 统一站内通知：审批到达、超时、到期提醒
- [ ] **操作日志审计** — 关键数据修改留痕，支持按人员/时间查询

### 中优先级
- [ ] **数据驾驶舱** — 管理层汇总看板：收入/支出/回款/开票一览
- [ ] **批量操作** — 批量审批、批量导出、批量打印
- [ ] **批量操作** — 批量审批、批量导出、批量打印

### 低优先级
- [ ] **移动端适配** — 审批流移动端优化，支持手写签名
- [ ] **数据导入** — Excel批量导入项目/合同/发票数据
- [ ] **打印模板自定义** — 用户可调整打印格式，不改代码

---

## 九、安全加固（技术债务）

### P0（立即处理）

- [ ] **SQL 注入风险排查** — `FinanceroleController.php:86, 218, 222, 226, 297-298` 等处 `FIND_IN_SET`/字符串拼接的 SQL，全部改为参数绑定或 `intval()` 白名单
- [ ] **数据库密码硬编码** — `config/db.php`、`xmtzxdb.php`、`fzrbsnwdb.php`、`web.php:68` 明文 root 密码迁移到 `.env` + `.gitignore`
- [ ] **删除/作废操作缺二次确认** — `contract/contractsTable.tsx`、`budget/dict/dictselect.tsx`、`order/OrderList.tsx` 等多处直接调删除 API，统一加 `Modal.confirm`

### P1（本周完成）

- [ ] **AES key/iv 硬编码** — `vue-project/src/utils/aes.ts:3-4` 改为 build-time 环境变量注入

---

## 十、性能优化（技术债务）

### P1（本周完成）

- [ ] **重型库动态导入** — `attandance/AttendanceList.tsx:10`、`CheckinDataList.tsx:10`、`useseal/UsesealList.tsx:8` 把 `import * as XLSX from 'xlsx'` 改为 `await import('xlsx')`，仅在导出按钮点击时加载（xlsx ~400KB）
- [ ] **N+1 查询修复** — `FinanceroleController.actionTemplatelist:362-399` 循环中 `getUserinfo($r['id'],'id')` 改为 `Userinfo::find()->where(['in','id',$ids])->indexBy('id')->all()`

### P2（两周内）

- [ ] **actionGetdict 重复全表扫描** — `FinanceroleController.php:471-489` 5 次 `find()->where(['>','id',0])->all()` 合并为单次查询或加 Redis 缓存
- [ ] **首屏串行改并行** — `budget/index.tsx` 6 个接口 `getcatogorystat/getheaders/getstat/getstattotal/home/refreshprojectreceived` 改 `Promise.all`

---

## 十一、代码质量（技术债务）

### P0（立即处理）

- [ ] **console.log 残留清理** — 财务模块 ~40 处调试日志（`UEditorComponent.tsx:49`、`budget/project/reportview.tsx:44,60,118`、`budget/balance/add.tsx:69`、`contract/addc.tsx:122` 等），集中删除或替换为统一 logger

### P2（两周内）

- [ ] **FinanceroleController 拆分** — 当前 2152 行 / 46 个 action 混合角色/流程/字典/付款，按业务拆为 `RoleController` / `FlowTemplateController` / `FinanceTemplateController` 等
- [ ] **巨型组件拆分** — `budget/index.tsx` (1263 行) / `contract/listc.tsx` (1020 行) 拆为子组件 + 提取 `useProjectList` 等 custom hook
- [ ] **moment 替换为 dayjs** — `budget/flow/add.tsx` 等直接 `import moment`，统一替换（vue-project 已用 dayjs）
- [ ] **service 层加类型定义** — 96 个 `request()` 调用散在 28 个 service.ts 全 `any`，补 `services/api.d.ts` 接口
- [ ] **yii2 _request 集中校验** — 当前所有 action 直接 `$this->_request['xxx']`，用 Yii2 Model/Form 类集中校验
- [ ] **流程模板 JSON schema 校验** — `FinanceroleController.php:367` `json_decode` 后直接遍历，损坏数据会 NPE，加 `Tools::modelRules` 校验
- [ ] **actionFlowalteritem/addsigner 公共方法抽取** — 两者逻辑 70% 重复，抽 `alterFlowNode($mode, $data)`

### P3（远期规划）

- [ ] **命名规范修正** — `mysite/src/pages/finance/attandance/` 拼写错误（应为 attendance），`FinanceroleController` 实际为流程+角色+字典混合体需拆分

---

## 十二、UX 交互改进

### P1（本周完成）

- [ ] **提交按钮 loading + 防重** — `contract/ledger/add.tsx`、`invoice/add.tsx`、`budget/balance/add.tsx` 在 await 期间按钮仍可点，加 `submitting` state + `disabled`
- [ ] **错误提示统一封装** — service.ts 的 `request()` catch 内仅 `console.log(e)`，用户看不到反馈，service 层统一 `message.error('保存失败')`

### P2（两周内）

- [ ] **表单前端校验** — `budget/project/addpro.tsx`、`budget/target/addtarget.tsx`、`company/add.tsx` 仅做必填，补 ProForm `rules`（金额格式、长度、范围）
- [ ] **审批历史查询接口** — 现有 `getflowinfo` 只返回当前状态，增加 `/api/financerole/flowhistory` 分页历史接口

---

## 十三、移动端（vue-project）补全

### P2（两周内）

- [ ] **vue-project 流程模块空骨架补全** — `views/press/`、`useseal/`、`finance/`、`photodispatch/` 多数 index.vue 只有 Tabs + List 占位，缺数据加载逻辑（目前仅 useseal 实现了 search），按需补全
- [ ] **collection-progress-dashboard（合同回款看板）** — `docs/superpowers/plans/collection-progress-dashboard-plan.md` 已设计但未实现，P1 级功能优先
- [ ] **collection-progress-gantt（合同回款甘特图）** — `docs/superpowers/specs/collection-progress-gantt-design.md` 已设计
- [ ] **project-lifecycle-timeline（项目生命周期时间线）** — `docs/superpowers/specs/project-lifecycle-timeline-design.md` 已设计

---

## 实施优先级参考

| 优先级 | 功能 |
|--------|------|
| **P0**（立即开始） | SQL 注入排查、数据库密码移 env、删除操作加确认、console.log 清理 |
| **P1**（近期完成） | xlsx 懒加载、N+1 修复、AES key 移 env、提交按钮防重、错误提示封装、合同回款看板 |
| **P2**（规划中） | FinanceroleController 拆分、巨型组件拆分、moment→dayjs、service 类型化、actionGetdict 缓存、vue-project 流程模块补全、合同变更记录、付款条件提醒 |
| **P3**（远期规划） | 命名规范修正、可视化流程设计器、电子签章集成、数据驾驶舱 |

---

## 十四、操作日志审计调研（不实现）

> 现状调研结果，按重要性排序，先不改

### 现有实现

- **方式**：自定义基类方法 `ApiBase::_operationlog()`（`modules/api/commons/ApiBase.php:204-234`），由 controller **显式手动调用**，非 Yii2 `Behavior`
- **存储**：两张 MySQL 表
  - `fzrbs_operation_log`（模型 `FzrbsOperationLog.php`）— 主表
  - `fzrbs_operation_log_params`（模型 `FzrbsOperationLogParams.php`）— 存请求参数 JSON
- **记录字段**：`userid / username / realname / catalog / url / remark / ip / inserttime` + 副表 `params`（密码自动脱敏）
- **缺点**：完全靠人工调用易遗漏；无 diff（旧值/新值对比）；`catalog/remark` 是文本，结构化差；无防删除/防篡改

### 覆盖情况

- **已覆盖（抽样）**：`Canteen / Budget / Salary / SalaryBonus / TruckOrder / Housing / LeaveFlow / Oauser / Admin / Role / RouteMenu / Vote / Information / Suggest / FlowRole / OperationLogParam` 等约 16+ 控制器
- **完全没覆盖**（0 处调用）：
  - `ContractController`（合同主控制器，**OA 核心**）
  - `QyfinanceController`（财务流程）
  - `FinanceroleController`（财务角色/模板）
  - `ContractnoticeController`（仅催办 2 处）
- **部分覆盖**：`BudgetController` 仅在 `1343 / 1392 / 4447 / 744` 等少数节点调用，核心钱/合同状态/决算均未留痕

### 待留痕清单（按重要性排序）

#### P0（本周必做，钱/合同核心）

| 控制器.action | 文件:行号 | 业务影响 |
|---|---|---|
| `ContractController::actionSavecontract` | `ContractController.php:289` | 新增/修改合同（金额、对方、付款条款） |
| `ContractController::actionDelcontract` | `ContractController.php:551` | 删除合同 |
| `ContractController::actionLock` / `actionNullify` | `ContractController.php:233/258` | 锁定/作废合同 |
| `ContractController::actionSavepaycollection` | `ContractController.php:1226` | 录入合同收款 |
| `ContractController::actionSaveinvoice` | `ContractController.php:1383` | 开票登记 |
| `ContractController::actionSaveledger` / `actionDelledger` | `ContractController.php:725/527` | 台账写入/删除 |
| `ContractController::actionAgree` / `actionReject` / `actionCancel` | `ContractController.php:1763/1860/1882` | 审批通过/驳回/撤销 |
| `ContractController::actionStartdeal` / `actionAltercharger` | `ContractController.php:2187/2162` | 启动收债/改负责人 |
| `BudgetController::actionSavebalance` | `BudgetController.php:3233` | 决算保存 |
| `BudgetController::actionDelbalance` | `BudgetController.php:3090` | 删除决算 |
| `BudgetController::actionSaveinvoicecheck` / `actionPaycollectioncheck` | `BudgetController.php:3057/2981` | 收款核销/发票核验 |
| `BudgetController::actionAlterproject` / `actionAltercreator` / `actionAltersubmitdate` | `BudgetController.php:1186/2357/1225` | 改项目字段/创建人/提交日期 |
| `BudgetController::actionLockpro` | `BudgetController.php:2326` | 锁定预算项目 |
| `BudgetController::actionCancel` | `BudgetController.php:4425` | 撤销预算审批 |
| `BudgetController::actionOfflineAgree` | `BudgetController.php:622` | 线下审批通过 |

#### P1（近期完成，权限/流程模板）

| 控制器.action | 备注 |
|---|---|
| `QyfinanceController::action*` | 财务审批全套（流程号、金额、动作） |
| `FinanceroleController::action*` | 财务角色/模板（角色ID、成员变更） |
| `FlowTemplateController::action*` | 流程模板（模板ID、节点变更） |
| `FlowRoleController::action*` | 流程角色（角色ID、成员变更） |
| `RoleController::action*` | 已有部分留痕但内容单薄（仅 username），建议补全菜单权限前后对比 |

#### P2（远期规划）

- `LeaveFlowController::actionApprove/Reject/Cancel/Revoke`（请假审批结果与销假）
- `AttendanceController` 异常打卡修正
- `OauserController` 用户禁用/启用

### 根本性建议

1. **基类改造**：把 `_operationlog` 升级为 `Behavior`（`beforeSave/afterSave` 自动触发），调用方无需手写，避免遗漏
2. **字段增强**：在 `fzrbs_operation_log` 增加 `old_value / new_value` JSON 列，behavior 自动写入，省去 remark 字符串拼接
3. **查询端**：`OperationLogController::actionIndex` 已支持多条件筛选，前端列表基本可用，重点是先把数据补全


