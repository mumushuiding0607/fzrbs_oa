# 待办文件 - 项目优化与功能建议

> 分析日期：2026/07/16
> 分析范围：mysite (React/UmiJS)、vue-project (Vue 3/Vite)、yii2.fznews.com.cn/basic (Yii2 PHP)




---

## 二、功能建议

### 【高优先级】

#### 1. 缺少财务模块的报表/BI分析功能
**现状：** 有大量数据但无可视化分析中心
**建议：** 集成ECharts/AntV构建财务仪表盘（月度营收趋势、部门预算执行率、合同逾期分布）



#### 3. 缺少工作流自动化（规则引擎）
**现状：** WorkflowParse.php只能匹配固定模板
**建议：** 添加规则引擎，示例：
- 金额>10万自动触发法务审核



### 【中优先级】



#### 7. 缺少API版本管理
**现状：** 所有API无版本前缀
**建议：** 实施`/api/v1/`、`/api/v2/`版本控制

#### 8. 缺少审计追踪可视化
**现状：** 有FzrbsOperationLog表但无可视化
**建议：** 合同/审批的时间线视图

#### 9. 缺少微信生态深度整合
**现状：** `vue-project`已集成`@wecom/jssdk`但只用基础API
**建议：** 集成企业微信客户联系、通讯录同步、日程API

#### 10. 缺少低代码/表单设计器
**现状：** 流程模板需要代码开发
**建议：** 拖拽式表单设计器（参考form-generator）

#### 11. 移动端缺少数据看板
**现状：** `HomeView.vue` 2294行接近组件大小上限
**建议：** 独立数据看板页面，关键指标卡片

---

### 【低优先级】

#### 12. 缺少国际化（i18n）支持
#### 13. 缺少数据备份/导出机制
#### 14. 缺少AI辅助功能（智能合同审查、智能问答）

---

## 三、数据库优化建议

### 【高优先级】缺失数据库索引

```sql
-- 合同表
ALTER TABLE fzrbs_contract ADD INDEX idx_state (state);
ALTER TABLE fzrbs_contract ADD INDEX idx_signdeptid (signdeptid);
ALTER TABLE fzrbs_contract ADD INDEX idx_creator (creator);
ALTER TABLE fzrbs_contract ADD INDEX idx_serial (serial);
ALTER TABLE fzrbs_contract ADD INDEX idx_type (type);

-- 预算表
ALTER TABLE fzrbs_budget_project ADD INDEX idx_state (state);
ALTER TABLE fzrbs_budget_balance ADD INDEX idx_projectid (projectid);
ALTER TABLE fzrbs_budget_balance ADD INDEX idx_moneytype (moneytype);

-- 审批流
ALTER TABLE weixin_oa_approval_info ADD INDEX idx_status (status);
ALTER TABLE weixin_oa_approval_info ADD INDEX idx_agentid (agentId);

-- 回款
ALTER TABLE fzrbs_contract_paycollection ADD INDEX idx_contractid (contractid);
ALTER TABLE fzrbs_contract_paycondition ADD INDEX idx_contractid (contractid);
```

---

## 四、实施优先级矩阵

| 优先级 | 任务 | 预期收益 | 实施难度 |
|--------|------|----------|----------|
| **极高** | Yii2 N+1查询优化 | API响应-50% | 低 |
| **极高** | 添加数据库索引 | 查询性能-70% | 低 |
| **极高** | 引入缓存层 | DB负载-60% | 中 |
| **高** | 替换FIND_IN_SET为关联表 | 关键查询-90% | 高 |
| **高** | mysite大页面拆分 | 首屏-30% | 中 |
| **高** | 启用MFSU | 开发体验++ | 中 |
| **中** | moment→dayjs迁移 | bundle-200KB | 中 |
| **中** | 集成财务BI仪表盘 | 用户价值++ | 高 |
| **中** | 移动端离线审批 | 移动用户++ | 高 |
| **中** | 全文搜索 | 用户价值++ | 中 |
| **低** | i18n国际化 | 长期价值 | 中 |
| **低** | AI辅助功能 | 前瞻性 | 高 |

---

## 五、快速见效清单（Quick Wins）

| 任务 | 工作量 | 收益 |
|------|--------|------|
| 添加数据库索引 | 1天 | 性能立刻提升 |
| N+1查询批量改写 | 3天 | ContractController接口提速 |
| 字典/部门数据加缓存 | 2天 | Dashboard响应提速 |
| xlsx从require改为静态import | 0.5天 | 导出功能更稳定 |
| mysite listc.tsx拆分Modal | 2天 | 组件维护性提升 |
| vue-project keepAlive限制max | 0.5天 | 内存占用减少 |

---

## 六、后续行动计划

1. **第一周**：添加数据库索引 + N+1查询优化
2. **第二周**：添加Redis缓存层（部门/字典/角色）
3. **第三周**：mysite大页面拆分 + xlsx优化
4. **第四周**：启用MFSU或升级UmiJS
5. **持续**：根据业务需求推进功能建议
